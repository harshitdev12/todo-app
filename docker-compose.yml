version: '3.8'

services:
  todo-app:
    build: .
    container_name: todo-app
    ports:
      - "5000:5000"
    volumes:
      - ./app.py:/app/app.py
      - ./templates:/app/templates
    environment:
      - FLASK_ENV=development
    restart: unless-stopped
